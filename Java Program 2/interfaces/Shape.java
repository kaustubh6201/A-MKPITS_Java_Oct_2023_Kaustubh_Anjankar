package com.mkpits.interfaces;

// interface is a class which is used to achieve 100% abstraction 
public interface Shape 
{
	// by default the method created in interface is abstract
	double getArea();
}
