const map = new Map([
    ['key1', 'value1'],
    ['key2', 'value2'],
    ['key3', 'value3']
    ]);
    
    for (const [key, value] of map) {
    // console.log(`Key: ${key}, Value: ${value}`);
    // console.log(key)
    console.log(`Key: ${key} `);
    }
    